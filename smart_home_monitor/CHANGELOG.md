## 1.4.6
- При включении Telegram сразу проверяются пороги и отправляется сообщение если значение вышло за границы
- Проверка при включении игнорирует 30-минутный антиспам

## 1.4.5
- Текст Telegram показывается как редактируемое значение по умолчанию
- Кнопка ↺ возвращает исходный текст

## 1.4.4
- Поля текста Telegram сохраняются при вводе (oninput)
- Серый текст в полях — подсказка, можно писать поверх

## 1.4.3
- TG переименован в Telegram везде
- Текст TG → Текст Telegram
- При включении Telegram сразу запускается проверка порогов

## 1.4.2
- Исправлен синтаксис JS — сломанный template literal в блоке метрик

## 1.4.1
- Исправлено: убрана дублирующаяся функция esc() которая ломала JS

## 1.4.0
- Настройки: добавлен интервал проверки порогов (по умолчанию 5 мин)
- Изменение интервала применяется сразу без перезапуска

## 1.3.9
- Сервер: добавлена переменная {time} в тексты сообщений порогов

## 1.3.8
- TG чекбокс заменён на переключатель, по умолчанию выключен
- Поля текста сообщений Мин/Макс всегда видны под каждой метрикой
- Переменные в тексте: {value} {unit} {min} {max} {name} {time}

## 1.3.7
- Убраны все эмодзи-иконки из списка устройств и модала

## 1.3.6
- Убрана иконка 📡 при ошибке загрузки картинки

## 1.3.5
- Убраны эмодзи из списка устройств — только картинки или пусто

## 1.3.4
- _serverModels объявлен через var для доступа из глобального скопа

## 1.3.3
- Исправлена инициализация _serverModels — убран лишний IIFE wrapper

## 1.3.2
- Исправлен порядок инициализации — _serverModels теперь доступен при первом рендере

## 1.3.1
- Картинки устройств: если есть серверная картинка — показывается сразу при рендере, иначе эмодзи

## 1.3.0
- Картинки устройств обновляются точечно после загрузки с сервера — без мигания и повторного рендера

## 1.2.9
- Исправлено отображение картинок — URL строится каждый раз с текущим BASE путём

## 1.2.8
- Исправлено отображение картинок устройств в списке — убран лишний wrapper

## 1.2.7
- Исправлен 404 для /images-list — эндпоинт перемещён до wildcard роута /{ieee}

## 1.2.6
- Картинки загружаются строго до рендера списка устройств

## 1.2.5
- Картинки загружаются до рендера устройств — не пропадают при F5

## 1.2.4
- Исправлен запуск: добавлен python-multipart для загрузки файлов

## 1.2.3
- Картинки устройств автоматически скачиваются с zigbee2mqtt.io при обнаружении устройства
- Картинки хранятся на сервере по модели — не теряются при обновлении страницы

## 1.2.2
- Картинки устройств сохраняются на сервере по модели (TS0601, WXKG02LM и т.д.) — не теряются при обновлении
- Одна картинка автоматически применяется ко всем устройствам одной модели

## 1.2.1
- Исправлена ошибка при клике на табы в "История уведомлений"

## 1.2.0
- Исправлена ошибка в "История уведомлений" — функция esc() не была доступна при первом рендере

## 1.1.9
- История уведомлений: ошибка теперь показывает причину, а не просто "Ошибка"
- Сервер: ошибки БД в alerts не роняют запрос

## 1.1.8
- Исправлена ошибка "_alertFilter is not defined" в История уведомлений

## 1.1.7
- Быстрое открытие устройств: кэш метрик 60 сек в памяти (как в 1.1.0)
- Метрики предзагружаются в фоне при открытии страницы — первый клик мгновенный

## 1.1.7
- Быстрый запрос метрик при открытии устройства — вместо медленного сканирования всех устройств

## 1.1.5
- 🗑 Кнопка «Очистить всё» в История уведомлений
- 🔢 Версия аддона в настройках теперь актуальная (динамически из API)

## 1.1.1
- 🔍 Поиск по Zigbee устройствам — по имени, модели, производителю

## 1.1.0 — Первый стабильный релиз

### Основные возможности
- 📡 Мониторинг Zigbee устройств — статус онлайн/офлайн, LQI, батарея
- 🔋 Заряд батарей — график разряда, пагинация
- ⚡ Токопотребление — мощность, ток, напряжение, энергия с графиком
- 📈 Конструктор графиков — любые метрики, период от 3 часов до 30 дней
- 🔔 История уведомлений — пагинация, автоудаление через 24ч
- 📨 Telegram — алерты офлайн, батареи, превышения порогов, ежедневный отчёт
- 🖼 Картинки устройств из Zigbee2MQTT или свои
